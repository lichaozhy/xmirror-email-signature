{
	"name": "@xmirror/email-signature-workspace",
	"private": true,
	"scripts": {
		"deps:install": "npm -v && npm install && lerna bootstrap",
		"build:docs": "rm -rf ./docs && lerna run build && git add . && mv ./packages/email-signature/dist ./docs",
		"preversion": "npm run build:docs"
	},
	"workspaces": [
		"packages/**"
	],
	"devDependencies": {
		"lerna": "^6.1.0"
	}
}
